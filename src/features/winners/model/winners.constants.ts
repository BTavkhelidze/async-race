import type { SortOrder, WinnerSortField } from '../types/winner.types';

export const WINNERS_PER_PAGE = 10;

export const DEFAULT_WINNER_SORT_FIELD: WinnerSortField = 'wins';

export const DEFAULT_WINNER_SORT_ORDER: SortOrder = 'DESC';
